<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:id="@+id/parent_layout">

    <com.mikhaellopez.circularimageview.CircularImageView
            android:id="@+id/profile_picture"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/profile_picture"
            app:civ_border_color='#EEEEEE'
            app:civ_border_width='4dp'
            app:civ_shadow="true"
            app:civ_shadow_radius="10"
            app:civ_shadow_color="#8BC34A" />
    <TextView
            android:layout_width="178dp"
            android:layout_height="61dp"
            android:text="About Me"
            android:id="@+id/about_me_title"
            android:layout_toRightOf="@+id/profile_picture"
            android:textColor="#000"
            android:textSize="40dp"

            android:layout_marginLeft="16dp" android:layout_marginTop="37dp"
            android:layout_marginBottom="53dp" android:layout_alignBottom="@+id/profile_picture"
            android:layout_alignTop="@+id/profile_picture" android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true" android:layout_marginRight="40dp" android:layout_marginEnd="40dp"
            android:layout_toEndOf="@+id/profile_picture" android:layout_marginStart="16dp"/>


</RelativeLayout>